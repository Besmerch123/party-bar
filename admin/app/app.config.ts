export default defineAppConfig({
  ui: {
    colors: {
      primary: 'blue',
      neutral: 'zinc'
    },
    input: {
      slots: {
        root: 'w-full'
      }
    },
    selectMenu: {
      slots: {
        base: 'w-full'
      }
    }
  }
});
